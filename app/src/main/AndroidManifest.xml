<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prime_paradise">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Prime_Paradise">
        <activity android:name=".CameraActivity">
        </activity>
        <activity android:name=".ProductexpandActivity"></activity>
        <activity
            android:name=".SampleScrollingActivity"
            android:label="@string/title_activity_sample_scrolling"
            android:theme="@style/Theme.Prime_Paradise.NoActionBar" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FurnitureActivity" />
        <activity android:name=".ClothActivity" />
        <activity android:name=".GuestActivity" />
        <activity android:name=".GrocActivity" />
        <activity android:name=".ElecActivity" />
        <activity android:name=".FurnActivity" />
        <activity android:name=".WearableActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".NextActivity" />
        <activity android:name=".WebViewActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".ShoeActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>